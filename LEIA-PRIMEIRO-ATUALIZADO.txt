PACOTE CORRIGIDO

Ajustes principais desta entrega:
1. Seleção de fornecedor pendente do operador corrigida.
   - As NFs pendentes agora carregam pelo índice em memória, sem depender de JSON embutido no <option>.
   - Corrige o erro "Fornecedor pendente inválido".
2. Agendamento interno do operador passa a gravar em "A DEFINIR".
   - Se a doca padrão não existir, o sistema cria automaticamente.
3. Painel de docas ajustado.
   - A fila "A DEFINIR" passa a exibir o controle de definição de doca.
   - O botão fica habilitado quando o status estiver CHEGOU.
4. Dados de exemplo removidos dos campos principais do operador.
5. docas.json atualizado para incluir a doca técnica "A DEFINIR" no fallback por arquivo.

COMANDOS:
1) Na raiz do projeto:
   npm install
2) Subir a aplicação:
   npm start
3) Acessar:
   http://localhost:3000

LOGIN DE TESTE EM FALLBACK:
   admin@local.test
   123456
