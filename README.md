# Agendamento de Descarga - pacote pronto para Hostinger

Este pacote já vem com backend e frontend no mesmo domínio.

Estrutura:
- `backend/` -> aplicação Node.js completa
- `backend/public/` -> frontend já pronto
- `backend/data/` -> base inicial em JSON
- `docs/` -> instruções de deploy

Credenciais iniciais:
- Email: `admin@local.test`
- Senha: `123456`
