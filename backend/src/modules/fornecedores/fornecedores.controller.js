import { prisma } from '../../config/prisma.js'; import { createCrudController } from '../shared/crud-factory.js'; import { createSchema, updateSchema } from './fornecedores.schemas.js'; const model = prisma.fornecedor; export const controller = createCrudController({ model, createSchema, updateSchema });
