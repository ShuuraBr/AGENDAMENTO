import { prisma } from '../../config/prisma.js'; import { createCrudController } from '../shared/crud-factory.js'; import { createSchema, updateSchema } from './docas.schemas.js'; const model = prisma.doca; export const controller = createCrudController({ model, createSchema, updateSchema });
